"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fontFamily = void 0;
exports.fontFamily = {
    name: `font-family`,
    initialValue: '',
    prefix: false,
    type: 1 /* PropertyDescriptorParsingType.LIST */,
    parse: (_context, tokens) => {
        const accumulator = [];
        const results = [];
        const quoted = [];
        let isQuoted = false;
        tokens.forEach((token) => {
            switch (token.type) {
                case 20 /* TokenType.IDENT_TOKEN */:
                    accumulator.push(token.value);
                    break;
                case 0 /* TokenType.STRING_TOKEN */:
                    isQuoted = true;
                    accumulator.push(token.value);
                    break;
                case 17 /* TokenType.NUMBER_TOKEN */:
                    accumulator.push(token.number.toString());
                    break;
                case 4 /* TokenType.COMMA_TOKEN */:
                    results.push(accumulator.join(' '));
                    quoted.push(isQuoted);
                    isQuoted = false;
                    accumulator.length = 0;
                    break;
            }
        });
        if (accumulator.length) {
            results.push(accumulator.join(' '));
            quoted.push(isQuoted);
        }
        return results.map((result, index) => quoted[index] || result.indexOf(' ') !== -1
            ? `'${result
                .replace(/\\/g, '\\\\')
                .replace(/'/g, "\\'")
                .replace(/\n/g, '\\a ')
                .replace(/\r/g, '\\d ')
                .replace(/\f/g, '\\c ')}'`
            : result);
    }
};
